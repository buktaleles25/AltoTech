# ลายน้ำน่ารัก 🎀 — Batch Image Watermark PWA

เว็บแอพใส่ลายน้ำรูปภาพ **หลายรูปพร้อมกันในคลิกเดียว** สไตล์น่ารักพาสเทล (kawaii)
ทำงานบนเครื่องคุณ **100%** — รูปไม่ถูกอัปโหลดขึ้นเซิร์ฟเวอร์เลย เหมาะกับใช้บนมือถือ (โดยเฉพาะ iPhone)

> A cute, mobile-first Progressive Web App to watermark many photos at once, entirely in your browser. No uploads, full privacy.

---

## ✨ ฟีเจอร์

- 📤 **อัปโหลดหลายรูปพร้อมกัน** (หรือรูปเดียว) — ลาก-วาง / เลือกจากเครื่อง / ถ่ายจากกล้อง
- ✍️ **ลายน้ำข้อความ** — ปรับข้อความ ฟอนต์ (Mali/Itim/ระบบ/Serif) ขนาด สี ความโปร่งใส เส้นขอบ
- 🎀 **ลายน้ำโลโก้** — อัปโหลด PNG (พื้นหลังโปร่งได้) ปรับขนาด/ความโปร่งใส (ใช้คู่กับข้อความได้)
- 📐 **ตำแหน่ง** — 9 จุด (มุม/กลาง) + โหมด **ปูทั้งรูป (tile)** + หมุน + ระยะขอบ
- 🪄 **ลบพื้นหลังด้วย AI + เปลี่ยนพื้นหลัง** — ลบพื้นหลังในเบราว์เซอร์ (โมเดล BiRefNet) แล้ว **เซฟเป็น PNG โปร่งใส** หรือใส่พื้นหลังใหม่: โปร่งใส / สีพื้น / ชุด preset (พาสเทล+สตูดิโอ 14 แบบ) / อัปโหลดเอง
- 👀 **พรีวิวสด** — ปรับแล้วเห็นผลทันทีบนรูปตัวอย่าง
- ⚡ **ใส่ลายน้ำทั้งหมดในคลิกเดียว** — มี progress bar + ยกเลิกได้
- 💾 **เซฟง่ายบนมือถือ** — แชร์เข้า Photos (iOS) / ดาวน์โหลด ZIP / บันทึกรายรูป
- 📱 **ติดตั้งเป็นแอพ (PWA)** — Add to Home Screen ใช้ offline ได้
- 🔒 **ความเป็นส่วนตัว** — ประมวลผลด้วย Canvas + AI บนเครื่อง ไม่มีการส่งรูปออก
- 🖼️ แก้การหมุนรูปตาม **EXIF** (รูปแนวตั้งจาก iPhone ไม่ตะแคง) และปรับขนาด/คุณภาพไฟล์ส่งออกได้

## 🆓 ฟรี / โอเพนซอร์ส 100%

ทุก dependency เป็น **MIT / Apache-2.0 / OFL** — ไม่มีค่าใช้จ่าย ไม่มี API เสียเงิน ไม่มีไลเซนส์จำกัด (ใช้เชิงพาณิชย์ได้):
โมเดลลบพื้นหลัง **BiRefNet (`onnx-community/BiRefNet_lite-ONNX`) = MIT**, transformers.js = Apache-2.0, React/fflate/Tailwind = MIT, ฟอนต์ = OFL, โฮสต์ฟรีบน GitHub Pages

## 🧩 เทคโนโลยี

Vite + React + TypeScript + Tailwind CSS v4 · `vite-plugin-pwa` · `fflate` (ZIP) · Web Share API · ฟอนต์ไทย `@fontsource` (Mali/Itim)
ลบพื้นหลัง: **`@huggingface/transformers`** รัน **BiRefNet** ในเบราว์เซอร์ (WebGPU + fallback WASM) ผ่าน Web Worker
ประมวลผลภาพด้วย **Canvas API** ล้วน — ไม่มี backend

> **หมายเหตุการลบพื้นหลัง:** ครั้งแรกจะดาวน์โหลดโมเดล AI (ต้องต่ออินเทอร์เน็ตครั้งแรก) แล้ว cache ไว้ใช้ offline ได้ · เร็วบนเครื่องที่มี WebGPU (Chrome/Edge/Safari 18+/iOS 18+), เครื่องอื่นใช้ WASM (ช้ากว่าแต่ทำงานได้) · รูปยังคงประมวลผลบนเครื่อง ไม่ถูกอัปโหลด

## 🚀 เริ่มต้นใช้งาน (dev)

```bash
npm install
npm run dev        # เปิด http://localhost:5173/AltoTech/
```

คำสั่งอื่น:

```bash
npm run build      # ตรวจ type + build ขึ้น dist/
npm run preview    # ดู production build ที่ http://localhost:4173/AltoTech/
npm run test       # รัน unit test (geometry / sizing / presets)
```

## 🌐 Deploy ขึ้น GitHub Pages

โปรเจกต์มี GitHub Actions (`.github/workflows/deploy.yml`) ที่ build + deploy ให้อัตโนมัติ

**ตั้งค่าครั้งเดียว:**

1. ไปที่ repo → **Settings → Pages**
2. ที่ **Source** เลือก **GitHub Actions**
3. push โค้ด (หรือกด Run workflow) — Actions จะ build แล้ว deploy ให้
4. เปิดใช้งานที่ 👉 **https://buktaleles25.github.io/AltoTech/**

> หมายเหตุ: `base` ใน `vite.config.ts` ตั้งเป็น `/AltoTech/` ให้ตรงชื่อ repo (ตัวพิมพ์ต้องตรง เพราะ Pages เป็น case-sensitive) หากย้าย repo ต้องแก้ค่านี้

## 📲 วิธีใช้บน iPhone

1. เปิดลิงก์แอพใน **Safari**
2. เลือกรูป (จาก Photos หรือถ่ายใหม่) → ปรับลายน้ำ → กด **"ใส่ลายน้ำทั้งหมด"**
3. กด **"แชร์เข้า Photos"** → เลือก **Save X Images** เพื่อเซฟลงคลังรูป
   - หรือ **"ดาวน์โหลด ZIP"** (เซฟลงแอพ Files)
4. อยากใช้บ่อย? แตะปุ่มแชร์ของ Safari → **Add to Home Screen** เพื่อติดตั้งเป็นแอพ

## ✅ QA Test Checklist

| # | เคส | คาดหวัง |
|---|-----|---------|
| 1 | อัปโหลดรูปเดียว | แสดง preview + การ์ดในแกลเลอรี |
| 2 | อัปโหลดหลายรูปพร้อมกัน | เพิ่มครบทุกรูป, นับจำนวนถูก |
| 3 | ปรับข้อความ/สี/ขนาด/ความโปร่งใส | preview อัปเดตทันที |
| 4 | สลับฟอนต์ (รวมภาษาไทย) | ตัวอักษรเปลี่ยนฟอนต์ถูกต้อง |
| 5 | เปิดลายน้ำโลโก้ + อัปโหลด PNG | โลโก้ปรากฏ (ซ้อนกับข้อความได้) |
| 6 | เลือกตำแหน่งครบ 9 จุด | ลายน้ำย้ายไปตำแหน่งที่เลือก ไม่ล้นขอบ |
| 7 | โหมดปูทั้งรูป (tile) + หมุน | ลายน้ำปูทั่วรูปแบบเอียงไม่มีช่องว่างมุม |
| 8 | กด "ใส่ลายน้ำทั้งหมด" | progress เดินหน้า, ทุกรูปขึ้นสถานะเสร็จ |
| 9 | ยกเลิกระหว่างประมวลผล | หยุดได้, รูปที่เหลือไม่ถูกประมวลผล |
| 10 | ดาวน์โหลด ZIP | ได้ไฟล์ .zip มีรูปครบ ลายน้ำถูกต้อง |
| 11 | แชร์/บันทึกรายรูป | ได้รูปเดี่ยวที่มีลายน้ำ |
| 12 | เปลี่ยน JPG/PNG + คุณภาพ + ขนาดสูงสุด | ไฟล์ผลลัพธ์เปลี่ยนตาม |
| 13 | รูปแนวตั้งจาก iPhone (EXIF) | ไม่ตะแคง |
| 14 | iOS: แชร์เข้า Photos | เซฟลงคลังรูปได้ |
| 15 | รีเฟรชหน้า | ค่าตั้งค่าล่าสุดยังอยู่ (จำใน localStorage) |
| 16 | Add to Home Screen + offline | เปิดใช้ได้แบบ PWA |
| 17 | เปิด "ลบพื้นหลัง (AI)" ครั้งแรก | โหลดโมเดล (มี progress) แล้ว preview เป็นตัวแบบพื้นโปร่ง |
| 18 | เลือกพื้นหลัง โปร่งใส/สีพื้น/preset/อัปโหลด | preview เปลี่ยนพื้นหลังตามเลือก |
| 19 | โปร่งใส → ใส่ลายน้ำทั้งหมด → บันทึก | ได้ **PNG โปร่งใส** (ลายน้ำทับถูก) |
| 20 | ลบพื้นหลัง + preset + ปรับค่าลายน้ำ | ปรับลายน้ำแล้วไม่รัน AI ซ้ำ (ใช้ cutout ที่ cache) |

## 📁 โครงสร้างโปรเจกต์ (ย่อ)

```
src/
├── lib/           # หัวใจ: watermark.ts (วาด), geometry.ts (ตำแหน่ง), zip.ts, share.ts, ...
├── hooks/         # useImages, useSettings, useProcessor
├── components/    # UI ทั้งหมด (kawaii)
├── i18n.tsx       # ไทย/อังกฤษ
└── types.ts
```

---

ทำด้วย 💕 · ประมวลผลบนเครื่องคุณเอง ไม่มีการอัปโหลด
